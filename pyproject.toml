[project]
name = "gd-orphans"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "gltflib>=1.0.13",
    "ruff>=0.14.6",
]

[dependency-groups]
dev = [
    "ruff>=0.14.6",
]
